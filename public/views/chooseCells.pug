extends layout

block content
    - var cellValues = [3, 6, 10, 12, 18, 20, 24, 32, 36, 40];
     form(action="/getSerialId" method='get' name="chooseCellsForm")
        h1 Choose No. Cells: 
        each numCell, i in cellValues
            input(name="numCells" type="submit", value=numCell)
            if ((i + 1) % 3 == 0)
                p